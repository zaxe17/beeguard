# Flask
FLASK_ENV=development
FLASK_DEBUG=1
PORT=8000
SECRET_KEY=change-me-in-prod
JWT_SECRET=change-me-in-prod-too
JWT_EXPIRES_HOURS=24

# MySQL (XAMPP defaults)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=beeguard_system

# CORS
FRONTEND_ORIGIN=http://localhost:3000

# ── NEW: SMTP for email verification ─────────────────────
# For Gmail: enable 2FA → create App Password → paste below
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=johnevansgutierrez9@gmail.com
SMTP_PASSWORD=nifm rlts nmuu bhdp
SMTP_FROM=BeeGuard <no-reply@beeguard.local>
SMTP_USE_TLS=1

# ── NEW: OTP behavior ────────────────────────────────────
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=60

